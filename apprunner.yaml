version: 1.0
runtime: python3
build:
  commands:
    - pip install --no-cache-dir -r requirements.txt
    - pip install gunicorn

run:
  command: gunicorn -w 4 -b 0.0.0.0:$PORT application:app
  network:
    port: 8080
