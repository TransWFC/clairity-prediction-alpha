version: 1.0
runtime: python3
build:
  commands:
    - "yum install -y gcc gcc-c++ make lapack-devel blas-devel"
    - "pip install --no-cache-dir -r requirements.txt"
run:
  command: "gunicorn -b 0.0.0.0:$PORT server:app"
  network:
    port: 8080