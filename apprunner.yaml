version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.11
    commands:
      - pip install -r requirements.txt
  build:
    commands:
      - echo "Build phase complete!"

  post_build:
    commands:
      - echo "Deploying app..."
      - gunicorn -w 4 application:app  

artifacts:
  files:
    - application.py
network:
  port: 8080
